class FacinatingNumber 
{
	public static void main(String[] args) 
	{
		int num =327;
		String str = num+""+(num*2)+(num*3); // we can multiply facinating number only 2 oe 3
		boolean flag = true;
		//loop which starts from 1 to 9 characters
		for(char j='1';i<='9';i++)
		{
			int cnt = 0; // inti char count is equalsto zero for every new char.
			//fetch char from string one by one from index 0.
			for(int j=0;j<ste.length();j++)
			{
				char ch = =str.charAt(j); /// char fetched is stored in var ch
				if(ch==i) // if i char matches with ch char if executes
				cnt++; //increases the count if cnd satisfy
			}
			if(cnt!=1) // if cnt of a char is not equals to 1 then if executes{
			{
				flag = false;  // int i the flag is cnt is not equals to 1
				break;  // terminating the loop
			}
		}
		if(flag){
			System.ou.println(num+ " is facinatingNumber.");
		}
		else
		{
			System.ou.println(num+ " is  not facinatingNumber.");
		}

	}
}
